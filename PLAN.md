# Token Monsters Plan

## Project
- GitHub owner: `aundal`
- Repository: `opencode-token-monsters`
- npm package: `@aundal/opencode-token-monsters`
- Purpose: OpenCode plugin showing token usage in the TUI sidebar.

## Current Work
- Package existing local Token Monsters plugins for npm install via OpenCode `plugin` config.
- Create README with install and usage instructions.
- Verify package build/pack.
- Push to GitHub and create a GitHub release.

## Future Tasks
- Publish to npm after npm auth is available.
